import { useState } from "preact/hooks";
import { FunctionComponent } from "preact";
import { setCookie } from "../utils/cookieutils.ts";

export type DataProps={
   
}
const Button: FunctionComponent<DataProps>=({})=>{
   
    return(
        <div>
  
        </div>
    )
}
export default Button;